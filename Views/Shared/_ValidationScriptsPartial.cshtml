@* Validation scripts (jQuery, jQuery Validate, jQuery Validate Unobtrusive)
   are already loaded once in _Layout.cshtml. This partial is kept only so
   existing @section Scripts { RenderPartialAsync("_ValidationScriptsPartial") }
   calls in views don't need to change. *@