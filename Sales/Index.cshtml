@model IEnumerable<InventoryManagementSystem.Models.Sale>

@{
    ViewData["Title"] = "سجل المبيعات";
    int currentPage = ViewBag.CurrentPage;
    int totalPages = ViewBag.TotalPages;
}

<h2>سجل المبيعات</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <form method="get" class="d-flex">
        <input type="text" name="search" value="@ViewBag.Search" class="form-control me-2" placeholder="ابحث باسم العميل..." />
        <button type="submit" class="btn btn-outline-primary">بحث</button>
    </form>
    <a asp-action="Create" class="btn btn-success">+ عملية بيع جديدة</a>
</div>

@if (!Model.Any())
{
    <p class="text-muted">مفيش عمليات بيع مسجلة لسه.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>التاريخ</th>
                <th>العميل</th>
                <th>عدد الأصناف</th>
                <th>الإجمالي</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var sale in Model)
            {
                <tr>
                    <td>@sale.SaleID</td>
                    <td>@sale.SaleDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@sale.CustomerInfo</td>
                    <td>@sale.SaleItems.Count</td>
                    <td>@sale.TotalAmount.ToString("0.00")</td>
                    <td><a asp-action="Details" asp-route-id="@sale.SaleID" class="btn btn-sm btn-info">عرض الفاتورة</a></td>
                </tr>
            }
        </tbody>
    </table>

    @if (totalPages > 1)
    {
        <nav>
            <ul class="pagination">
                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-search="@ViewBag.Search">@i</a>
                    </li>
                }
            </ul>
        </nav>
    }
}
